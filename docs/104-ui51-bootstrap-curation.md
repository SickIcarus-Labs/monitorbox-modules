# UI51 #104 — real bootstrap metrics, single regeneration, one arrange handle

Date: 2026-09-26. Supersedes the UI50 physical follow-up without rewriting signed UI50.

**Module-touch preflight (before implementation):** Primary **#104**, fix remaining usability/auto-population deficits surfaced in operator's physical iPad acceptance of UI50. Dividend **#103**, retain and rerun header convergence regression, observe physically during the same acceptance session. Reviewed same-module backlog #94, #91 and #85; #94 item-picker and manual composition are already present and not reopened; #85 cross-version fail-closed Core boundary is outside this UI-only radius. No Core runtime/provider changes.

## Explicit operator-approved scope
- Replace both UI50 reset buttons with **one Regenerate dashboard** action. Regenerate selected site by default, optionally all sites when multiple are configured. Always request affirmative confirmation and stage, never automatically mutate existing sites on upgrade. Preserve other sites when selected-site only; never reset graphs/monitoring/module secrets. Retained snapshots remain immutable. One optimistic-concurrency Validate → Preview → Apply for the whole draft.
- Bootstrap default generation must be capability-aware but conservative and provider-neutral: hosts explicitly added (and designated gateway where eligible) initially show their status plus up to a small, stable number of genuinely available CPU, memory, storage and network metrics. Only select a measurement when the canonical public object/component actually exposes a numeric value; never fabricate it, assume vendor-specific metric names, duplicate generic values, show unrelated giant raw inventories, or select 24 stress metrics by default. Prefer live series only if actually advertised and stable on canonical catalogue access; otherwise use truthful controller-state cadence. Native Network/Cameras/Power/Internet card summaries already curate their respective monitored members and should not duplicate readings absent concrete benefit. Same default generator must serve fresh bootstrap, explicit regeneration, and default homepage.
- Preserve the ability to add 24 or 64 manual items, 1Hz live-card interest, hidden-source truth, existing site/network/camera/UPS grouping, cards/snapshots already personalized, and 3/2/1 responsive reading order. Do not silently change saved card preferences during upgrade or snapshot restore. New automatic placement should **measure actual rendered card heights in the live current view**, or use a bounded deterministic estimate matching selected default rows and stable initial row-major admission. No jump on 1Hz telemetry.
- Arrange mode: **one visible drag grip per card**, tap it for accessible directional/column fallback, keyboard arrows supported. Place the control in a dedicated compact strip **above, not over, the native card title or health indicator**. No secondary Move button. Move actions remain operable on iPad Safari via pointer capture. Clear stale 'Loading the actual homepage preview…' after native render succeeds and show a real ready indication; fail closed on missing native preview.
- Add synthetic Playwright acceptance for regeneration selected site/all sites, truthful CPU/memory/storage/network defaults, missing metrics and discovery-only suppression; touch drag, tap-grip move/Undo, same-homepage card DOM and dimensions, 3/2/1, legacy v1-v4 snapshots, #103 health race. Separate paired accepted real-Core 2.6.0 managed-loader/revisions/RecoveryManager qualification and final populated physical iPad acceptance are required before beta/stable.

## Publication and safety
Use immutable **UI v1.15.0 build51**, successor to signed official-dev UI50 (v1.14.0+50, SHA-256 d978bea3ab5170d515b131f9bb7a8a5ca311ac0c7746da0b9db0dd259d6c4b03); declare trusted main dev-supersession authority commit 001cc6bacceeb17e76d1f428b5cbc68b1709895d. UI50 signed bytes remain intact. Publish only development artifact via repository-trusted CI after exact-head gates. No production deployment, Main merge, beta/stable/latest promotion, appliance reboot, or automatic destructive operation without operator's exact authorization and full backup.
